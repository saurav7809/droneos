# ═══════════════════════════════════════════════════════════════
# DroneOS — Production Application Configuration
# Activated via: SPRING_PROFILES_ACTIVE=prod
# ═══════════════════════════════════════════════════════════════

spring:
  application:
    name: drone-backend

  # ── PostgreSQL ────────────────────────────────────────────────
  # RenderDatabaseUrlPostProcessor (runs before this file is read) will
  # automatically convert any postgres:// or postgresql:// URL set in
  # SPRING_DATASOURCE_URL to the correct jdbc:postgresql://host:5432/db
  # format.  These YAML values are only used as LOCAL dev fallbacks.
  datasource:
    url: ${SPRING_DATASOURCE_URL:jdbc:postgresql://${DB_HOST:localhost}:${DB_PORT:5432}/${DB_NAME:dronedb}}
    driver-class-name: org.postgresql.Driver
    username: ${DB_USERNAME:postgres}
    password: ${DB_PASSWORD:postgres}
    hikari:
      minimum-idle: 2
      maximum-pool-size: 10
      idle-timeout: 600000
      max-lifetime: 1800000
      connection-timeout: 30000
      pool-name: DroneOSPool

  # ── JPA / Hibernate ───────────────────────────────────────
  jpa:
    hibernate:
      ddl-auto: update
    defer-datasource-initialization: true
    show-sql: false
    open-in-view: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        default_schema: public
        jdbc:
          lob:
            non_contextual_creation: true
  sql:
    init:
      mode: never

  # ── Disable H2 console ─────────────────────────────────────
  h2:
    console:
      enabled: false

server:
  port: ${PORT:8080}
  compression:
    enabled: true
    mime-types: application/json,application/javascript,text/css
  error:
    include-stacktrace: never
    include-message: never
  # Security headers from Tomcat
  tomcat:
    max-threads: 200
    accept-count: 100

# ── JWT (override via environment variable!) ───────────────────
jwt:
  secret: ${JWT_SECRET:CHANGE_ME_IN_PRODUCTION_AT_LEAST_256_BITS_LONG}
  expiration: 86400000   # 24 hours

# ── AI Service ────────────────────────────────────────────────
ai-service:
  url: ${AI_SERVICE_URL:http://ai-service:5000}

# ── Actuator health endpoints (for Render health checks) ──────
management:
  endpoints:
    web:
      exposure:
        include: health,info
      base-path: /actuator
  endpoint:
    health:
      # "always" lets Render's unauthenticated health-check probe see {"status":"UP"}
      # without this the probe gets 401 and Render marks the service as unhealthy
      show-details: always
  health:
    db:
      enabled: true

# ── Logging ───────────────────────────────────────────────────
logging:
  level:
    com.droneos: INFO
    org.springframework.security: WARN
    org.springframework.web.socket: WARN
    org.hibernate.SQL: WARN
  pattern:
    console: "%d{yyyy-MM-dd HH:mm:ss} [%level] %logger{36} - %msg%n"
